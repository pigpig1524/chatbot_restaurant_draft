## This draft project is built for testing restaurant chatbot's feature

Main features are placed at `copilot_ultils.py`

For the scratch repo, view `sample_rag_1.zip`

### Local deployment
Execute the command `streamlit run copilot.py`
